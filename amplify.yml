version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "Removing Windows-specific dependency..."
        - npm uninstall @rollup/rollup-win32-x64-msvc
        - echo "Installing dependencies..."
        - npm install
    build:
      commands:
        - echo "Building with production API URL..."
        - echo "VITE_API_URL=https://area25-simple.eba-b42mgv5j.eu-north-1.elasticbeanstalk.com/api" > .env.production
        - npm run build
  artifacts:
    baseDirectory: dist
    files:
      - '**/*'
  cache:
    paths: []