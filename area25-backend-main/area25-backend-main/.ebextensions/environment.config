option_settings:
  aws:elasticbeanstalk:application:environment:
    PORT: 4000
    NODE_ENV: production
    CORS_ORIGIN: https://6th-space.com,https://www.6th-space.com
    # Database and AWS credentials should be configured directly in the AWS console
    # for security reasons. Do not commit sensitive data to version control.